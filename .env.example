# Copy to .env to override defaults. `make up` creates .env with PUBLIC_URL if missing.
PUBLIC_URL=http://localhost:9080

# Optional overrides (defaults are fine for a local lab)
# DEMO_PASSWORD=Passw0rd!
# KEYCLOAK_ADMIN_PASSWORD=admin
# APISIX_ADMIN_KEY=local-apisix-admin-key
# MCP_TOKEN_EXCHANGE_SECRET=local-mcp-exchange-secret
# SSO_BROKER_SECRET=local-sso-broker-secret
# CONFIRMATION_SIGNING_KEY=local-confirmation-key-change-me
